import { Router } from 'express';

const router = Router();

// TODO: Implement timeLog routes
router.get('/', (req, res) => {
  res.json({ message: 'TimeLogs routes - Coming soon!' });
});

export default router;
